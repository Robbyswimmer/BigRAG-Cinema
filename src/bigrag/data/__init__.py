"""
bigrag.data -- Data ingestion, validation, embedding, and Parquet I/O.

This sub-package owns every step between the raw Kaggle CSV and the
analysis-ready Parquet files that contain pre-computed embeddings.
"""
